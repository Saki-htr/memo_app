<form action="/memos/<%= @memo[0]["id"] %>" method="post">
  <input type="hidden" name="_method" value="PATCH">
  <div>
    <input type="text" name="memo_title" value = <%= h (@memo[0]["title"]) %> required>
  </div>
  <div>
    <textarea type="text" name="memo_content" rows="5" required input><%= h(@memo[0]["content"]) %></textarea>
  </div>
  <div>
    <input type="submit" value="変更">
  </div>
</form>
