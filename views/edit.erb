<form action="/memos/<%= @memo["id"] %>" method="post">
  <input type="hidden" name="_method" value="PATCH">
  <div>
    <input type="text" name="memo_title" value = <%= h (@memo["title"]) %> required>
  </div>
  <div>
    <textarea type="text" name="memo_content" rows="5" required input><%= h(@memo["content"]) %></textarea>
  </div>
  <div>
    <input type="submit" value="変更">
  </div>
</form>
