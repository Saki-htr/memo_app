
<form action="/memos/new" method="get">
  <input type="submit" value="追加">
</form>
<ul>
  <% @memos.each do |memo|  %>
    <li><a href="/memos/<%= memo["id"] %>"><%= h(memo["title"]) %></a></li>
  <% end %>
</ul>
